# Cursor reads `.cursorignore` at repo root (same syntax as .gitignore).
# Rules in `ai.cursorignore` are NOT applied by Cursor unless copied here.

data/

outputs/
outputs_baseline/
outputs_improved/

checkpoints/
__pycache__/

*.pth
*.pt

# Uncomment to hard-block all PNG from AI (also blocks @-mention of figures):
# *.png
